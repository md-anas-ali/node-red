node_modules
npm-debug.log*
.git
.gitignore
.env
.env.*
*.md
.DS_Store

# Runtime data that must never be baked into the image or leak into
# the build context — Node-RED's own runtime writes live in /data
# inside the container, never in the repo.
data/
flows.json
flows_cred.json
.config.runtime.json
.config.nodes.json
.config.users.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editors / OS
.vscode/
.idea/
*.swp
